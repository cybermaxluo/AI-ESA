# 排除所有 .DS_Store 文件（无论在哪个目录下）
**/.DS_Store
.DS_Store

# 排除所有 .claude 目录及其内容（无论在哪个路径下）
**/.claude/
.claude/

# 排除所有路径下的 settings.local.json（只要文件名匹配就排除）
**/settings.local.json

# 额外：排除 .claude 相关的文件（若有单个文件以 .claude 结尾）
*.claude
